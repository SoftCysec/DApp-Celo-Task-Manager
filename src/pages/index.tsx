import NavBar from '../components/NavBar';
import TaskItem from '../components/TaskItem';
import AddTaskModal from '../components/AddTaskModal';

function Home() {
    // State for tasks, modal, etc.
    // Handlers for adding, toggling, and deleting tasks

    return (
        <div>
            <NavBar />
            {/* List out tasks using TaskItem */}
            {/* Trigger AddTaskModal */}
        </div>
    );
}

export default Home;
